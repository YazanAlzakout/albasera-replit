diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
index 0cdcc0b..c9ccfdc 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/player/VideoPlayerAudioTracks.kt
@@ -60,7 +60,8 @@ class VideoPlayerAudioTracks(owner: VideoPlayer) : VideoPlayerListener {
 
         if (MimeTypes.isAudio(format.sampleMimeType)) {
           formatsToGroups[format] = group.mediaTrackGroup to i
-          val track = AudioTrack.fromFormat(format) ?: continue
+          val isSupported = group.isTrackSupported(i)
+          val track = AudioTrack.fromFormat(format, isSupported) ?: continue
           availableAudioTracks.add(track)
         }
       }
diff --git a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
index 20dcbb7..e185b6c 100644
--- a/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
+++ b/node_modules/expo-video/android/src/main/java/expo/modules/video/records/Tracks.kt
@@ -32,10 +32,11 @@ class SubtitleTrack(
 class AudioTrack(
   @Field val id: String,
   @Field val language: String?,
-  @Field val label: String?
+  @Field val label: String?,
+  @Field val isSupported: Boolean = true
 ) : Record, Serializable {
   companion object {
-    fun fromFormat(format: Format?): AudioTrack? {
+    fun fromFormat(format: Format?, isSupported: Boolean = true): AudioTrack? {
       format ?: return null
       val id = format.id ?: return null
       val language = format.language
@@ -44,7 +45,8 @@ class AudioTrack(
       return AudioTrack(
         id = id,
         language = language,
-        label = label
+        label = label,
+        isSupported = isSupported
       )
     }
   }
diff --git a/node_modules/expo-video/build/VideoPlayer.types.d.ts b/node_modules/expo-video/build/VideoPlayer.types.d.ts
index 204f98d..d7e3b4a 100644
--- a/node_modules/expo-video/build/VideoPlayer.types.d.ts
+++ b/node_modules/expo-video/build/VideoPlayer.types.d.ts
@@ -551,5 +551,12 @@ export type AudioTrack = {
      * Label of the audio track in the language of the device.
      */
     label: string;
+    /**
+     * Whether the device has a decoder capable of playing this audio track.
+     * A track can still appear in `availableAudioTracks` (it exists in the
+     * stream) while this is `false` if nothing on the device can decode it.
+     * @platform android
+     */
+    isSupported: boolean;
 };
 //# sourceMappingURL=VideoPlayer.types.d.ts.map
\ No newline at end of file
